import { Text, View } from 'react-native';
import CalculatorTemplate from '../modules/calculator/template/calculatorTemplate';
import { Stack } from 'expo-router';

const Page = () => {
  return <CalculatorTemplate />;
};

export default Page;
